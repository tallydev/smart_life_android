<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:paddingLeft="5dp"
    android:paddingRight="5dp">
    <Button
        android:id="@+id/count_editor_add"
        android:layout_width="20dp"
        android:layout_height="20dp"
        android:layout_centerVertical="true"
        android:background="@mipmap/ic_add"
        android:layout_alignParentLeft="true"/>
    <TextView
        android:id="@+id/count_editor_count"
        android:layout_width="wrap_content"
        android:layout_height="24dp"
        android:minWidth="36dp"
        android:gravity="center"
        android:background="@color/white"
        android:layout_toRightOf="@id/count_editor_add"
        android:textSize="@dimen/text_18"
        android:textStyle="bold"
        android:text="1"/>
    <Button
        android:id="@+id/count_editor_reduce"
        android:layout_width="20dp"
        android:layout_height="20dp"
        android:layout_centerVertical="true"
        android:background="@mipmap/ic_reduce"
        android:layout_toRightOf="@id/count_editor_count"/>
</RelativeLayout>